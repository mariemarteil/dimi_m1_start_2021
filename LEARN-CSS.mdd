#LEARN-CSS

## les variables en CSS
Possible de déclarere des variables en css (comme en JS donc !). 
Syntaxe est la suivante (double tiret précédent le nom de la variable) : 
```css
body {
  --grid-row: 4;
}
```css

